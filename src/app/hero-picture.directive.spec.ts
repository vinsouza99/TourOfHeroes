import { HeroPictureDirective } from './hero-picture.directive';

describe('HeroPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroPictureDirective();
    expect(directive).toBeTruthy();
  });
});
